export const fetchStayOptions = () => {

  // Add feature to save stayOptions to backend every time user is logged in and makes a search
  // User is able to see previous searches in searchform when searchbar is focused and no input is given
  
  return Promise.resolve({})

  // return $.ajax({
  //   url: '/api/options',
  //   method: 'get',
  //   data: {
  //     options
  //   }
  // });
};